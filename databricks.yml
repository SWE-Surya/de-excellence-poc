# This is a Databricks asset bundle definition for de_excellence_poc.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: de_excellence_poc
  uuid: d8c3313b-c3c6-4b7d-b8e9-8f6e3813a39a

include:
  - resources/*.yml

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-93f33ef9-9f51.cloud.databricks.com
    variables:
      catalog: de_excellence_poc
      schema: dev
  prod:
    mode: production
    workspace:
      host: https://dbc-93f33ef9-9f51.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/suryamani.sudhakar@cloudkaptan.com to make sure we only have a single copy.
      root_path: /Workspace/Users/suryamani.sudhakar@cloudkaptan.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: de_excellence_poc
      schema: prod
    permissions:
      - user_name: suryamani.sudhakar@cloudkaptan.com
        level: CAN_MANAGE
